export function PrototypeBanner() {
  return (
    <div className="w-full bg-warn/15 border-b border-warn/40 text-warn px-4 py-2 text-sm font-mono">
      <span className="font-semibold">⚠️ PROTOTYPE</span> — HeLa testnet only. No real
      funds. All tokens (mUSD / mTKA / mTKB) and the price oracle are mocks.
    </div>
  );
}
